"use strict";
function addNumber(num1, num2) {
    return num1 + num2;
}
function lowerToUpperCase(val) {
    return val.toUpperCase();
}
function signUp(name, email, password, isPaid = false) {
    return true;
}
function login(email, password) {
    return true;
}
const auth = (authToken) => {
    return authToken;
};
function isSubscribed(str) {
    console.log(str);
}
function handleErrors(msg) {
    throw new Error(msg);
}
